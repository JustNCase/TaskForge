{
  "name": "@taskforge/service-opportunity",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "start": "tsx src/index.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@taskforge/core": "workspace:*",
    "@taskforge/middleware": "workspace:*",
    "@taskforge/security": "workspace:*",
    "@taskforge/database": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.7.0",
    "typescript": "^5.3.2",
    "vitest": "^3"
  }
}
