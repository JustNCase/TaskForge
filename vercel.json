{
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter @taskforge/dashboard build",
  "framework": "nextjs",
  "outputDirectory": "apps/dashboard/.next",
  "rootDirectory": "apps/dashboard",
  "ignoreCommand": "npx turbo-ignore @taskforge/dashboard"
}
